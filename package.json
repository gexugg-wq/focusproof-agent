{
  "name": "focusproof-agent",
  "private": true,
  "version": "0.1.0",
  "description": "OpenHands-backed general learning-evidence verification application.",
  "scripts": {
    "test": "python -m pytest agent-server/tests -v",
    "lint:python": "python -m ruff check agent-server",
    "typecheck:python": "python -m mypy agent-server"
  }
}
